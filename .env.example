# Finnhub API key (free at finnhub.io)
FINNHUB_API_KEY=your_finnhub_key_here

# LM Studio OpenAI-compatible endpoint
LM_STUDIO_URL=http://localhost:1234/v1
LM_STUDIO_MODEL=local-model
LM_STUDIO_API_KEY=lm-studio

# Server
ASTRA_HOST=127.0.0.1
ASTRA_PORT=8765

# Trading
ASTRA_TICK_SECONDS=300
ASTRA_WATCHLIST_SIZE=25
ASTRA_MAX_POSITION_PCT=0.15
ASTRA_DAILY_LOSS_LIMIT_PCT=0.05
ASTRA_FEE_PER_TRADE=0.05
ASTRA_SLIPPAGE_BPS=5

# Auto-exit rules (enforced before the LLM decides)
ASTRA_STOP_LOSS_PCT=0.05
ASTRA_TAKE_PROFIT_PCT=0.15
ASTRA_TRAILING_STOP_PCT=0.08
ASTRA_MIN_CONFIDENCE=0.30

# Position discipline
ASTRA_MAX_OPEN_POSITIONS=10
ASTRA_TARGET_POSITION_PCT=0.10
ASTRA_MIN_TRADE_VALUE=25.0
ASTRA_COOLDOWN_MINUTES=60
ASTRA_ENTRY_MIN_CONFIDENCE=0.55
ASTRA_RISK_PER_TRADE_PCT=0.02

# Optional: priority tickers (always deep-dived). Comma-separated.
# Example: ASTRA_CUSTOM_WATCHLIST=NVDA,AMD,TSLA,AAPL,MSFT
ASTRA_CUSTOM_WATCHLIST=

# Universe scanner — when on, every tick scans the FULL S&P 500 with
# cached candles, ranks by signal strength + momentum, and deep-dives
# only the top N (plus held positions + custom watchlist). True by default.
ASTRA_SCAN_UNIVERSE=true
ASTRA_SCAN_TOP_N=30

# Simulation mode — synthetic market data so you can watch the bot trade
# when the real market is closed. Toggle live in Settings too.
ASTRA_SIMULATE_DATA=false

# Paths (relative to project root)
ASTRA_DATA_DIR=./data
